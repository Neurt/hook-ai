"""Tool / connector layer.

Thin, swappable adapters to the outside world. The stubs here let the whole
pipeline run with only an OpenRouter key; replace each with a real, compliant
provider (see ../../docs/feasibility.md for the sustainable options).
"""
